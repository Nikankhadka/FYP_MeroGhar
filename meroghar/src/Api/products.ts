import 'server-only'

//this way these api call only be called in the server components no in client component which is best next js practice 

//then create seperate cleint componnets to call in server component/page where the data will be hydrated
